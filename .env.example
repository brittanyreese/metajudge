# Copy to .env (gitignored) and fill in what you need.
# Only required for examples/audit_llm_judge.py --mode live.

# --provider gemini (default). Needs a billed (Tier 1+) Google AI project.
GOOGLE_AI_API_KEY=

# --provider openrouter. Free-tier :free models; capacity not guaranteed.
OPENROUTER_API_KEY=
